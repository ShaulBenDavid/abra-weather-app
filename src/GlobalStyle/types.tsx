export type Theme = {
  // Colors
  black: string;
  white: string;
  primary: string;
  secondary: string;
  fadeText: string;
  gray: string;
  grayBg: string;
  errorRed: string;
  yellow: string;
  // Fonts
  buttonDefaultSize: string;
  // Line Height
  buttonLineHeight: string;
};

export type LightModeTheme = {
  primary: string;
};
